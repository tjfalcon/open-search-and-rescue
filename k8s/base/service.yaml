apiVersion: v1
kind: Service
metadata:
  name: open-search-and-rescue
spec:
  selector:
    app.kubernetes.io/name: open-search-and-rescue
  ports:
    - name: http
      port: 80
      targetPort: http
